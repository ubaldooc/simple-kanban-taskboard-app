# Este es un archivo de ejemplo para las variables de entorno del frontend.
# Para el desarrollo local, copia este archivo a uno nuevo llamado .env.local
# y reemplaza los valores de ejemplo con tus propias claves.
#

# URL base del backend a la que el frontend hará las peticiones.
# En desarrollo, normalmente es el servidor local de Express.
# En producción, será la URL de tu API desplegada.
VITE_API_BASE_URL=http://localhost:5001

# ID de Cliente de Google para la autenticación de OAuth.
# Este valor es público y se obtiene desde la Google Cloud Console.
# Asegúrate de que las "Authorized JavaScript origins" en tu configuración de Google
# incluyan la URL de tu frontend (ej. http://localhost:5173).
VITE_GOOGLE_CLIENT_ID=TU_GOOGLE_CLIENT_ID_AQUI.apps.googleusercontent.com